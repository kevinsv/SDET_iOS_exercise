language: objective-c
osx_image: xcode10.2
branches:
  only:
    - CI
    - master
before_install:
  - carthage update
  - gem install fastlane --no-ri --no-rdoc --no-document
install: true
script:
  - fastlane scan -s LoginDemoRxRedux